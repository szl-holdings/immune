name: hf-exact-receipt-delete

on:
  push:
    branches: [main]
    paths:
      - .github/workflows/hf-consolidate-one-shot.yml
      - scripts/hf_space_delete_from_receipt.py
  workflow_dispatch: {}

permissions:
  contents: read

concurrency:
  group: hf-exact-receipt-delete
  cancel-in-progress: false

jobs:
  delete-exact-preserved-set:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    env:
      HF_TOKEN: ${{ secrets.HF_TOKEN }}
      HF_HUB_DISABLE_TELEMETRY: "1"
      HF_HOME: /tmp/hf-home
    steps:
      - name: Checkout exact protected-main transaction
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          persist-credentials: false

      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97
        with:
          python-version: "3.12"

      - name: Install pinned Hub client
        run: python -m pip install --disable-pip-version-check --no-cache-dir "huggingface_hub==1.23.0"

      - name: Compile exact deletion transaction
        run: python -m py_compile scripts/hf_space_delete_from_receipt.py

      - name: Fail closed if Hugging Face credential is absent
        shell: bash
        run: |
          set -euo pipefail
          test -n "${HF_TOKEN:-}" || { echo "::error::HF_TOKEN absent"; exit 2; }
          echo "::add-mask::$HF_TOKEN"

      - name: Delete only the immutable receipt-bound non-flagship set
        run: python scripts/hf_space_delete_from_receipt.py
